services:
  - type: web
    name: ai-resume-api
    env: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: node dist/migrations/run.js && node dist/index.js
    healthCheckPath: /api/health
